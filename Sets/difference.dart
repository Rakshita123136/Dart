//diference : returns elements in one set that are not in the other
void main() {
  var setA = {1, 2, 3};
  var setB = {3, 4, 5};
  var differenceset = setA.difference(setB);
  print(differenceset);
}
