//List Properties:
//.length
//.isEmpty
//.isNotEmpty
//.first
//.last
void main() {
  var numbers = [20, 10, 30, 40];
  print(numbers.length);
  print(numbers.isEmpty);
  print(numbers.isNotEmpty);
  print(numbers.reversed);
  print(numbers.last);
}
